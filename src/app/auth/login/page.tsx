import React from "react";
import LoginInputField from ".";

const Login = () => {
  return (
    <div className="w-full h-screen flex items-center justify-center">
      <LoginInputField />
    </div>
  );
};

export default Login;
